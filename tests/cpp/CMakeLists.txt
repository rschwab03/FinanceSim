# C++ tests
# Add test files here as they are created

# Example (uncomment when adding tests):
# find_package(GTest REQUIRED)
#
# add_executable(financesim_tests
#     test_event_bus.cpp
#     test_models.cpp
# )
# target_link_libraries(financesim_tests
#     financesim_models
#     GTest::gtest_main
# )
#
# include(GoogleTest)
# gtest_discover_tests(financesim_tests)
